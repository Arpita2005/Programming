#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b,HCF,LCM;
    printf("Enter the two numbers:\n");
    scanf("%d%d",&a,&b);
    
    //a and b are two numbers.So the formula is : a*b=HCF(a,b)*LCM(a,b)


    


}