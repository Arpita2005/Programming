#include<stdio.h>
#include<conio.h>
int main()
{
    int size,arr[size],i,index,element;
    printf("Enter the size of the array :\n");
    scanf("%d",&size);
    printf("Enter The elements :\n");
    for(i=0;i<size;i++)
    {
        printf("Element %d :",i+1);
        scanf("%d",&arr[i]);
    }
    printf("\n======================\n");
    printf("The elements are :\n");
    for(i=0;i<size;i++)
    {
        printf("%d element is : %d",i+1,arr[i]);
    }

    printf("Enter the new element :\n");
    scanf("%d",&element);
    printf("Enter the index where the new wlwmwnt to be inserted :\n");
    scanf("%d",&index);
   while(i>index)
   {
     
   }
}